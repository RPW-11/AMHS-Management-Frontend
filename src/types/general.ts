export type Option = {
    name: string;
    value: string;
}

export interface NotificationData {
  userName: string;
  description: string;
  dayWithTime: string;
  date: string;
  isRead: boolean;
}